{
  "name": "form-builder-submission-processor",
  "version": "1.0.0",
  "description": "Cloudflare Worker — drains Redis queue and saves submissions to Turso",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.78.7"
  }
}
